require_relative '..lib/google_searcher.rb'

describe "#get_search_input_or_stop" do

it "stops the program if there is no input" do
    expect(ARVG).not_to (be_nil)
end

end

describe "#build_url" do

it "create an URL from an input" do 
    expect(build_url).to eq().to eq()
end

end
